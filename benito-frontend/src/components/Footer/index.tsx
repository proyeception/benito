import React from "react";
import { hot } from "react-hot-loader";
import "./styles.scss";

const Footer = (_: any) => (
  <div className="qui-footer mt-3">
    <div className="container">Un footer piola</div>
  </div>
);

export default hot(module)(Footer);
