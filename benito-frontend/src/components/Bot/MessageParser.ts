class MessageParser {
  actionProvider : any
  constructor(actionProvider:any) {
    this.actionProvider = actionProvider;
  }

  parse(message:String) {

    //this.actionProvider.handleProjectParse()
    
  };
}

export default MessageParser;
