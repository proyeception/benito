import React from "react";
import { hot } from "react-hot-loader";
import "./styles.scss";

const Header = (_: any) => (
  <div>
    <div>Lupita</div>
    <div>Proyectate</div>
    <div>Fotito del usuario</div>
  </div>
);

export default hot(module)(Header);
