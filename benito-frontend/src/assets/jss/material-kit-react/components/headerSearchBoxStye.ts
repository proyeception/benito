import { createStyles } from "@material-ui/core";

const headerSearchBoxStyle = createStyles({
  row: {
    marginBottom: "10px",
    marginTop: "10px",
  },
});

export default headerSearchBoxStyle;
